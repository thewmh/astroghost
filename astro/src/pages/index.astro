---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation';
import { ghostClient } from '../lib/ghost';
const page = await ghostClient.pages
  .read({
      slug: 'home'
  })
  .catch((err) => {
      console.error(err);
  });
if (!page) return;
---

<Layout>
  <!-- <section>
    <Fragment set:html={page.html} />
  </section> -->
</Layout>